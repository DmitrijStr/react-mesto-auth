const imageModal = document.querySelector('.popup_type_image');
const imageModalImage = imageModal.querySelector('.pop-up__image');
const imageModalPlace = imageModal.querySelector('.pop-up__image-name');
const ESC_KEYCODE = 27;

export const popupCloseButton = document.querySelector('.pop-up__btn_action_deny');

export { imageModal, imageModalImage, imageModalPlace, ESC_KEYCODE };